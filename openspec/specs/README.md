# OpenSpec Capability Specs

Each capability directory contains accepted behavior in `spec.md`. Capability
directory names are stable product semantics, not package names. Proposal
capability entries must name live capability directories exactly. The official
OpenSpec directory shape is authoritative; repository-private capability
metadata does not extend it.
