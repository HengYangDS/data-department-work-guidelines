# Tasks

## 1. Establish the material Change

- [x] 1.1 Create the official `ddwg-dual-forge-runner-convergence` Change and
      an ETHOS-owned scope companion with only its actual surfaces.
- [x] 1.2 Add proposal, design, repository-governance delta, active-claim
      contract, and dated Chronicle with explicit local/Forge evidence bounds.
- [x] 1.3 Validate the strict OpenSpec lifecycle and active claim binding.

## 2. Bind repository CI to independent runners

- [x] 2.1 Require GitHub self-hosted macOS/ARM64 plus the repository-specific
      runner variable; reject fork-origin pull requests on that host.
- [x] 2.2 Use local managed Node 22, Chrome, and Homebrew Python in the GitHub
      projection without hosted-Linux provisioning.
- [x] 2.3 Require the GitLab `ddwg-documentation-ci` tag while preserving its
      independent Docker executor and common verifier.
- [x] 2.4 Extend the CI contract test and governance documentation without
      calling any local or provider configuration a hosted result.

## 3. Verify local intent and define external acceptance

- [x] 3.1 Run focused workflow-binding, YAML, documentation, and proof checks.
- [x] 3.2 Complete the implementation commit and prepare the archive-HEAD
      proof and local `candidate/dev` to `dev`/`main` closeout. Actual archive
      and promotion remain blocked until the pre-existing historical archive
      identities are repaired through their own bounded Change.
- [x] 3.3 Define provider-specific deployment and hosted-run evidence; install,
      register, publish, and observe those external facts only after local
      closeout.
