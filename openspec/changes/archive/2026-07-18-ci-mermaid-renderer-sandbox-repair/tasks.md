# Tasks

## 1. Establish the material Change

- [x] 1.1 Create the official `ci-mermaid-renderer-sandbox-repair-20260718`
      Change and its product-defined scope companion.
- [x] 1.2 Add proposal, design, repository-governance delta, active claim, and
      Chronicle with separate local and hosted-evidence boundaries.

## 2. Repair the hosted renderer boundary

- [x] 2.1 Add a checked-in portable Puppeteer configuration containing the
      hosted-only Chrome compatibility argument.
- [x] 2.2 Add an explicit validator option that forwards only the canonical
      repository-relative configuration to Mermaid CLI and leaves local default
      rendering unchanged.
- [x] 2.3 Route GitHub and GitLab through the same bound verifier selection and
      extend CI contract positive and negative tests.
- [x] 2.4 Update governance documentation and the unreleased changelog without
      claiming provider completion.

## 3. Verify and close out

- [x] 3.1 Run focused renderer positive and negative checks, CI workflow syntax,
      and local documentation rendering.
- [x] 3.2 Run format, links, render, rollout, boundary, layout, scope, strict
      OpenSpec, ETHOS lifecycle, claims, freshness, and exact-HEAD proof.
- [x] 3.3 Archive through the official OpenSpec command, rerun proof at the
      archive result, and complete local candidate/accepted promotion.
- [x] 3.4 Publish only `dev` and `main`, then inspect GitLab and GitHub runs at
      the published SHA; hosted results remain distinct external evidence.
