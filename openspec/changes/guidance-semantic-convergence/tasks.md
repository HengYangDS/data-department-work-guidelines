# Tasks

## 1. Establish the semantic owners

- [x] 1.1 Create one official Change with proposal, design, and DDWG-owned delta
      specifications; verify
      `openspec validate guidance-semantic-convergence --type change --strict --json`
      passes.
- [x] 1.2 Map every active section and unique obligation in the former root
      guideline to one current owner or an explicit removal reason; verify no
      requirement is lost through a heading-by-heading diff review.
- [x] 1.3 Classify each historical tracked claim and Chronicle by unique fact,
      actual producer, consumer, and Git/OpenSpec/Attestation alternative;
      verify no sole historical fact is discarded and no unconsumed tracked
      proof root is retained by inertia.

## 2. Make the guidance usable

- [x] 2.1 Build the human, Agent, and documentation entrypoints around reader
      tasks; verify each route resolves to one current normative topic.
- [x] 2.2 Move analysis, decision, delivery, data qualification, communication,
      human-Agent collaboration, and rule evolution into cohesive topic owners;
      verify every mapped old obligation is present, rejected, or narrowed with
      a stated reason and no parallel normative copy.
- [x] 2.3 Remove the root monolith and stale deep links after updating known
      inbound consumers, profile normative sources, and release version binding;
      verify local links and version extraction against the new owner.
- [x] 2.4 Test representative member and Agent tasks for correct rule, scope,
      responsible actor, next action, and evidence limit; record baseline and
      new observations without claiming actual team adoption.

## 3. Retire false authorities and stale projections

- [x] 3.1 Consolidate decision records into lowercase files and one register;
      verify only enduring rationale is current, transient state has its real
      owner, numbering is never reused, and the five-section validator passes.
- [x] 3.2 Retire unsupported `evidence/`, duplicate indexes, the invalid
      evolution ledger, and empty scaffolds after unique-fact review; verify Git
      history and official archives retain the old bytes and current links
      resolve.
- [ ] 3.3 Replace copied ETHOS canonical capability placeholders through the
      official `retire_capabilities` transition and DDWG-owned specs; verify no
      live canonical spec asserts ETHOS product-family behavior.
- [x] 3.4 Correct current OpenSpec, ETHOS, contribution, and reference command
      examples; verify examples match the installed public CLI and no current
      page routes to a retired command.
- [x] 3.5 Select the GitLab canonical `ci-linux-arm64-docker` tag and update its
      contract test and live documentation; verify the new selector passes, the
      old selector fails, and the GitHub hosted workflow remains independent.

## 4. Verify and accept the source

- [x] 4.1 Replace fixed card and diagram quotas with checks for current links,
      metadata, every present render, command syntax, decision structure, and
      meaningful negative cases; verify focused regression tests pass.
- [ ] 4.2 Run locked-tool cold setup, documentation format/link/render, rollout
      and governance checks, official OpenSpec strict validation, ETHOS
      admission and changed-plan checks, and `git diff --check`; verify
      `node_modules` is untracked and the worktree is clean after commit.
- [ ] 4.3 Run full repository proof at the exact committed source HEAD, then
      integrate into the local candidate and accept into `dev` and `main`;
      verify current ETHOS receipts and exact branch OIDs without claiming
      remote delivery.

## 5. Deliver and close the declared Change

- [ ] 5.1 Verify a GitLab runner actually accepts the canonical tag and each
      Forge's Git transport works without prompts before publication; report any
      unavailable peer without weakening local acceptance.
- [ ] 5.2 Publish only eligible refs to GitLab and GitHub; verify both peers'
      exact source OID and successful hosted documentation jobs at that OID,
      with no remote `candidate/dev` or `work/*` refs.
- [ ] 5.3 Update this task carrier from the observed delivery facts, officially
      archive the completed Change, and run fresh proof and publication checks
      for the archive result; verify final branch and peer OIDs and retain any
      distinct hosted-CI gap as unproved rather than checking a future result in
      advance.
