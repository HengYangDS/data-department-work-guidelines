# Tasks

## 1. Establish the Change

- [x] 1.1 Start a leased Work Lane from the accepted candidate, inspect current
      navigation and links, and create this official OpenSpec Change.
- [x] 1.2 Strictly validate the proposal, design, and guidance-discovery delta;
      verify ETHOS selects this Change for the intended material paths.

## 2. Converge the reader entry

- [x] 2.1 Replace the root topic table with one handoff to the documentation
      map. Retain the historical boundary in that map and delete the redundant
      history page without editing archived source objects.
- [x] 2.2 Update the existing rollout check and its regression coverage for
      root-to-map and map-to-topic routes, including rejected duplicate and
      missing-handoff cases.

## 3. Validate and deliver

- [x] 3.1 Run format, Markdown lint, offline links, Mermaid rendering, rollout,
      repository-boundary tests, strict official OpenSpec, ETHOS changed
      planning, and `git diff --check`; review all current links and text.
- [ ] 3.2 Commit the admitted source with a valid signature, execute exact-HEAD
      ETHOS proof, accept it locally, then publish through ETHOS and verify
      GitLab and GitHub refs and CI independently at that object.
- [ ] 3.3 Reconcile the evidence with these tasks and inspect the owned lane
      before the official archive transition, preserving foreign work.
