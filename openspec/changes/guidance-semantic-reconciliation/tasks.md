# Tasks

## 1. Reconcile the guidance at its current owners

- [x] 1.1 Audit each distinct duty in the last unified guideline and retired
      draft against the design matrix; verify every retained, combined, or
      rejected item has a current owner or explicit user-authorized exclusion.
- [x] 1.2 Restore the confirmed gaps in the existing English topic pages;
      verify the evidence, diagnostic, Agent, management, and effective-time
      counterexamples can be answered without a root monolith or duplicate tree.
- [x] 1.3 Review all human and Agent routes after the edits; verify current links
      reach one topic owner and no fixed management cadence or outdated rule
      tree returns.

## 2. Publish one checked license

- [x] 2.1 Add the standard MIT grant with the verified copyright identity and
      align README, npm metadata, and lockfile; verify no Apache declaration or
      competing current license remains.
- [x] 2.2 Add positive and negative repository tests for missing or divergent
      license metadata; verify the focused test and public entry behavior.

## 3. Prepare the compatible release

- [x] 3.1 Review the normative diff against the public compatibility surface;
      verify a documented SemVer classification, matching `VERSION`, charter,
      and Keep a Changelog entry before the release tag is attempted.
- [x] 3.2 Run the release-identity and changelog negative tests; verify a
      malformed heading, drifting version, or wrong tag base fails.

## 4. Verify the source before native closeout

- [x] 4.1 Run the locked repository verifier, strict official OpenSpec
      validation, online dependency audit, and relevant negative tests; verify
      no failures and no generated dependency tree is staged.
- [x] 4.2 Review the staged patch against the semantic map, MIT grant, release
      links, and single-Change scope; run `git diff --cached --check` and
      `ethos plan --changed --json`, and verify every changed path is attributed
      to this Change. Commit, proof, archive, acceptance, publication, tag,
      releases, and lane retirement remain native effects, never pre-checked
      future task boxes.
