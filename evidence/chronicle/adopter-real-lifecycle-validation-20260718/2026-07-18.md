# Chronicle: real adopter lifecycle validation — 2026-07-18

## Observation

DDWG declares non-empty material path patterns and keeps only
`docs-integrity` and `markdown-format` in its code-correctness gate selection.
The lifecycle identifier is not a code-correctness gate, and no method package
is used as a governance carrier.

From an owned Work Lane, the repository-bound ETHOS adapter rejected a material
`guidelines.md` prewrite with no active Change. After the official OpenSpec
command created one new carrier, the same path remained rejected while its
Change-local companion was absent. These results establish the expected
fail-closed boundary for the current local exercise.

## Action

The official Change `adopter-real-lifecycle-validation-20260718` received the
only permitted bootstrap write: its exact untracked `scope.toml`. The companion
declares the Change artifacts, this Chronicle, its claim, and the canonical
repository-governance delta; it is ETHOS-owned companion data, not an OpenSpec
workflow-schema extension.

With that companion in the working tree, prewrite admitted the declared Change,
claim, Chronicle, and canonical-spec paths, including the companion itself.
It continued to reject `guidelines.md` because the Change does not cover it.

## Evidence Boundary

This record describes local command outcomes in one leased Work Lane at the
time of observation. It does not establish archive, candidate landing, accepted
closeout, GitLab/GitHub publication, hosted CI, rendered documentation, or
organizational adoption.

## Revisit

Run strict official OpenSpec validation, the repository-bound lifecycle and
claim readers, documentation verification, and exact-HEAD proof before local
landing. Archive and closeout require fresh observations of their resulting
heads; each remote remains a separate evidence plane.
