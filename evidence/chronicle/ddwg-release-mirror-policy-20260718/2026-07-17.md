# DDWG accepted-to-release mirror policy — 2026-07-17

## Claim

The repository will use ETHOS accepted-root closeout to move `dev` and its
release-facing `main` mirror to the same proved candidate SHA.

## Boundary

This record is local only. It excludes GitLab/GitHub publication, hosted CI,
provider branch protection, and organizational adoption.

## Evidence

- The existing policy classified `main` as an independent release branch.
- The new branch-role setting uses `release_mirror = "accepted_ff"`.
- `candidate/dev` remains a local candidate train and is not a remote target.

## Decision

Prove the exact policy-change HEAD, land it locally, then execute accepted-root
closeout so `main`, `dev`, and `candidate/dev` converge without a remote push.
