# Chronicle: CI Mermaid renderer sandbox repair — 2026-07-18

## Observation

GitHub Actions documentation runs for commit `c99383b` reached Mermaid rendering
and failed because the hosted Chrome process had no usable sandbox. This is a
provider runtime constraint observed after the earlier checkout repair, not a
document-content failure.

## Action

This Change adds a single checked-in Puppeteer configuration for hosted
CI, routes both provider projections through the same existing repository
verifier, and leaves local rendering without a Puppeteer override. Regression
tests reject an omitted, divergent, or inline provider override.

## Evidence Boundary

This Chronicle records the observed failure and local repair intent. It does not
prove archive, current-HEAD proof, local acceptance, GitLab or GitHub
publication, hosted rendering success, or organizational adoption.

## Revisit

After an eligible published ref triggers each provider, inspect the fresh run at
that SHA. If either renderer still fails, preserve its log and open a new Change
rather than expanding this CI-only exception.

## Archive Observation

On July 18, 2026, the official OpenSpec archive command moved the completed
`ci-mermaid-renderer-sandbox-repair-20260718` Change to
`openspec/changes/archive/2026-07-18-ci-mermaid-renderer-sandbox-repair-20260718`
and applied its requirement to the canonical repository-governance specification.
The claim is retained only as a historical record of that archive transition.

## Archive Evidence Boundary

This observation establishes only the official local archive result. It does not
establish current-HEAD proof, local acceptance, GitLab or GitHub publication,
hosted CI, remote rendering, or organizational adoption.
