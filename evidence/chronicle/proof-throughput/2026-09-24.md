# Chronicle: proof throughput — 2026-09-24

## Observation

The accepted ETHOS `dev` runtime at `709347f0ce0965e6a338aa5128c60c4d3f4044a8`
attributes declared material paths to one selected active official Change. Its
canonical repository-governance specification explicitly rejects `scope.toml`
and other parallel carriers as authorization. DDWG still carried the superseded
July companion assumption and nonstandard OpenSpec capability metadata.

## Action

The quiesced stale Lease was taken over through an accepted exact-CAS
Attestation, and the common immutable hook runtime was refreshed to the accepted
ETHOS source. This Change removes the private companion and obsolete metadata,
updates the repository contract and tests to active-Change attribution, and
keeps the GitHub-hosted migration separate from the unchanged GitLab runner.

## Evidence Boundary

This observation proves current local ownership, runtime binding, and the
contract discrepancy that required repair. It does not prove the final patch,
archive, exact-HEAD proof, local closeout, remote publication, or either Forge's
hosted execution.

## Revisit

Update the active claim only from completed local validation. Record remote
publication and hosted CI only after each Forge reports the exact published SHA.
