# Chronicle: DDWG OpenSpec archive identity normalization — 2026-07-21

## Observation

The current ETHOS archive audit rejected six July 18 archive paths because each
encoded a second temporal suffix inside the logical Change identifier. The
affected carriers were historical, but the invalid present path shape blocked
current repository lifecycle and proof commands, including the separately scoped
runner convergence Change.

## Action

This Change moved exactly six archive directories to the canonical
`YYYY-MM-DD-<date-free-logical-id>` form. Claims and Chronicles now name the new
carrier paths; historical logical identifiers, dated observations, evidence
boundaries, external-run identifiers, and task histories remain unchanged. The
profile uses the current typed adapter declaration, and a repository-native test
checks the exact six canonical paths and rejects retained legacy duplicates.

## Evidence Boundary

This record establishes a local archive-identity repair only. It does not
re-archive the six historical Changes, prove their current behavior, accept this
repair, deploy runners, publish to either Forge, or establish GitHub Actions or
GitLab CI execution.

## Revisit

Run the ordinary strict lifecycle, claim, and exact-HEAD proof gates. Archive
this current Change only after those checks pass, then re-prove its archive
result. Resume the independent dual-Forge runner Change only after this local
identity repair has been accepted through the normal branch lifecycle.

## Archive Observation

On July 20, 2026, the official OpenSpec archive command moved the completed
`ddwg-openspec-archive-identity-normalization` Change to
`openspec/changes/archive/2026-07-20-ddwg-openspec-archive-identity-normalization`
and applied its accepted requirement to the canonical repository-governance
specification.

## Archive Evidence Boundary

This observation establishes only the official local archive result. It does not
establish current-HEAD proof, candidate landing, accepted closeout, runner
deployment, GitLab or GitHub publication, or provider CI execution.
