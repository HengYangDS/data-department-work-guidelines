# Chronicle: adoption lifecycle repair — 2026-07-18

## Observation

The prior repository topology contained several archived Change and reconciliation
carriers for one governance correction. Those carriers were not an acceptable
substitute for one complete material Change. The July 12, 2026 work remains a
historical fact: it was not originally carried through OpenSpec.

On July 18, 2026, an owned Work Lane created the official active Change
`adoption-lifecycle-repair-20260714` from the current accepted local baseline.
The accepted ETHOS contract now supplies material-path admission; this repository
uses its declared `[openspec].material_paths` and the Change-local `scope.toml`
companion. The companion is product-defined ETHOS input, not an OpenSpec schema.

## Action

The active Change will replace date-named decision reports with stable DRs,
remove `docs/superpowers/` from live topology, remove legacy reconciliation
carriers, and bind local-first publication rules to portable repository checks.
The repository boundary validator is deliberately limited to document topology;
it does not certify lifecycle or replace ETHOS admission.

## Evidence Boundary

This observation proves only that the current repair was initiated through the
active Change and scoped by the accepted product contract. It does not prove
archive, local acceptance, remote publication, hosted CI, rendered documentation,
or real-work adoption.

## Revisit

Record later observations only after the matching current-HEAD command has run.
OpenSpec archive, local closeout, GitLab publication, GitHub publication, and
hosted CI each require separate evidence.
