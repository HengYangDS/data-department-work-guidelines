[meta]
version = 1
owner = "data-department"

[semantic_axes]
required = ["subject", "role", "state", "relations"]

[states]
allowed = ["canonical", "active", "planned", "experimental", "superseded", "archived"]

[roles]
allowed = [
  "index",
  "explanation",
  "reference",
  "decision",
  "policy",
  "evidence",
  "history",
  "template",
  "plan",
  "research",
  "findings",
  "progress",
  "ledger",
  "how-to",
]

# 内核目录由 ETHOS 统一约束；此处只声明本仓库的扩展目录。
[extension_roots]
"docs/governance" = ["policy", "explanation", "ledger", "index"]
